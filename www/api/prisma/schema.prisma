generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DB_URL")
}

model users {
    id   Int    @id @default(autoincrement())
    name String @db.Text
}
