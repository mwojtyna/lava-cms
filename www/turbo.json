{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"start": {
			"dependsOn": ["build"]
		},
		"dev": { "cache": false },
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".next/**"]
		}
	}
}
