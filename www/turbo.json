{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"start": {
			"dependsOn": ["build"]
		},
		"dev": { "cache": false },
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["admin/**", "frontend/**"],
			"outputs": ["dist/**", ".next/**"]
		}
	}
}
