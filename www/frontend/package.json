{
	"name": "frontend",
	"type": "module",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "http-server -p 3000 ./dist",
		"dev": "astro dev --port 3000",
		"build": "astro build",
		"preview": "astro preview",
		"astro": "astro"
	},
	"dependencies": {
		"api": "workspace:*",
		"astro": "^1.9.1",
		"http-server": "^14.1.1"
	},
	"devDependencies": {
		"prettier": "^2.8.1"
	}
}
