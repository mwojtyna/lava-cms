{
	"name": "frontend",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bin/deno run --allow-net --allow-read --allow-env ./dist/server/entry.mjs",
		"dev": "astro dev --port 3000",
		"build": "astro build",
		"lint": "tsc && eslint . && astro check",
		"preview": "astro preview",
		"astro": "astro"
	},
	"dependencies": {
		"@astrojs/deno": "^4.2.0",
		"@lavacms/client": "workspace:*",
		"astro": "^2.7.0"
	},
	"devDependencies": {
		"eslint-plugin-astro": "^0.27.2"
	}
}
