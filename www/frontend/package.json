{
	"name": "frontend",
	"type": "module",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "NODE_ENV=production http-server -p 3000 ./dist",
		"dev": "NODE_ENV=development astro dev --port 3000",
		"build": "astro build",
		"lint": "tsc && astro check",
		"preview": "astro preview",
		"astro": "astro"
	},
	"dependencies": {
		"api": "workspace:*",
		"astro": "^1.9.1",
		"http-server": "^14.1.1"
	},
	"devDependencies": {
		"prettier": "^2.8.1",
		"typescript": "^4.9.4"
	}
}
