{
	"name": "lava-cms",
	"version": "0.0.1",
	"scripts": {
		"start": "FORCE_COLOR=1 NODE_ENV=production turbo start",
		"dev": "FORCE_COLOR=1 NODE_ENV=development turbo dev",
		"build": "scripts/build.sh"
	},
	"dependencies": {
		"turbo": "^1.6.3"
	}
}
